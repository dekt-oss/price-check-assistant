-e .

# Streamlit Community Cloud caches installs from requirements.txt. Keep quote-upload
# runtime parsers explicit here so a pyproject-only dependency change cannot leave an
# existing deployment without the modules needed to open the quote-analysis page.
openpyxl>=3.1,<4
xlrd>=2.0,<3
pypdf>=5,<7
