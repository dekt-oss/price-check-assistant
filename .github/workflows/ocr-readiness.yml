name: OCR Readiness

on:
  pull_request:
  push:
    branches: [main]

jobs:
  synthetic-ocr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Tesseract language packs
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng tesseract-ocr-kor
      - name: Install project and test dependencies
        run: pip install -e '.[dev]'
      - name: Run real synthetic OCR readiness test
        run: python -m pytest -q tests/test_ocr_execution_readiness_real.py
